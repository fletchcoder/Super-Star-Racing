export function vehicleDescs(number) {
	if (number === 0) {
		return "A crossover with a decent amount of speed";
	} else if (number === 1) {
		return "A speedy sport bike that races to the finish";
	} else if (number === 2) {
		return "A supercar with incredible speed";
	} else if (number === 3) {
		return "An off-roading vehicle that climbs over rocks";
	} else if (number === 4) {
		return "A hatchback with decent speed";
	} else if (number === 5) {
		return "A convertible that travels smoothly";
	} else if (number === 6) {
		return "A sedan with a surprising amount of pep";
	} else if (number === 7) {
		return "A muscle car that moves quickly off the line";
	} else if (number === 8) {
		return "A hatchback that moves with nimble agility";
	} else if (number === 9) {
		return "A tractor that takes its time mowing to the line";
	} else if (number === 10) {
		return "A bicycle that pedals its way to the finish";
	} else if (number === 11) {
		return "A semi truck that barrels its way down the line";
	} else if (number === 12) {
		return "A city bus that's just trying to make its next stop";
	} else if (number === 13) {
		return "A construction machine that moves rocks and debris ";
	} else if (number === 14) {
		return "An armored tank that can destroy anything in its path";
	} else if (number === 15) {
		return "An army helicopter that flies over the competition";
	}
}
